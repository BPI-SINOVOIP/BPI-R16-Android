MCE_CLIENT_ID=$1

if [ -z "$MCE_CLIENT_ID" ]
then
   MCE_ERROR=1
fi


if [ "$MCE_ERROR" ]
then
   echo "usage: mce-startclient <CLIENT_ID>>"
   echo "
    Where CLIENT_ID is a unique identifier for the client
"
   exit 1
fi

MCE_ACTION="START_CLIENT --es CLIENT_ID $MCE_CLIENT_ID"

source include.sh